# ========== DATABASE CONFIGURATION ==========
# MySQL URL for Drizzle ORM or direct MySQL connection
DATABASE_URL=mysql://root:Suyog@4398sql@localhost:3306/authintegrate

# Individual MySQL connection variables (some modules might use these)
DB_HOST=localhost
DB_USER=root
DB_PASS=Suyog@4398sql
DB_NAME=authintegrate

# ========== SECURITY KEYS ==========
# Used for session encryption / JWT signing
SESSION_SECRET=supersecretkey123

# ========== SERVER CONFIG ==========
# The port where your backend server will run
PORT=5000

# Optional environment mode
NODE_ENV=development

# ========== HARDWARE CONFIGURATION (NEW) ==========
# CRITICAL: Must match the port where your ESP32 is connected
SERIAL_PORT=COM8
# CRITICAL: Must match the Serial.begin() baud rate in your C++ code (115200)
SERIAL_BAUD=115200